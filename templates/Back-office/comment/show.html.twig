{% extends 'base-back.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
   
<div class="col-md-6 grid-margin ">
<br>
 <h1  class="font-weight-bold">Comment</h1>
<br>
<br>
    <div class="card">
    <table class="table table-striped table-borderless">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ comment.author }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ comment.date }}</td>
            </tr>
            <tr>
                <th>Likes</th>
                <td>{{ comment.likes }}</td>
            </tr>
        </tbody>
    </table>
    </div>
{#    <a href="{{ path('app_comment_edit_back', {'id': comment.id}) }}" class="btn btn-warning">edit</a>#}
{#    {{ include('Back-office/comment/_delete_form.html.twig') }}#}
    <a href="{{ path('app_experience_show_back', {'id': comment.idExp}) }}" class="btn btn-primary">Show the experience</a>
    <a href="{{ path('app_comment_index_back') }}" class="btn btn-primary">back to list</a>

</div>
  
{% endblock %}
