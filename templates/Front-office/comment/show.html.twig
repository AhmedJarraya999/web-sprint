{% extends 'base-front.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ comment.author }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ comment.date }}</td>
            </tr>
            <tr>
                <th>Likes</th>
                <td>{{ comment.likes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment_index_front') }}">back to list</a>

    <a href="{{ path('app_comment_edit_front', {'id': comment.id}) }}">edit</a>

    {{ include('Front-office/comment/_delete_form.html.twig') }}
{% endblock %}
